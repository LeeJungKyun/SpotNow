package com.example.spotnow.common;

public class Coordinate {
    public double _latitude; // Variable to store the latitude value
    public double _longitude; // Variable to store the longitude value

    public Coordinate(double latitude, double longitude) {
        _latitude = latitude; // Initialize the latitude value
        _longitude = longitude; // Initialize the longitude value
    }
}
